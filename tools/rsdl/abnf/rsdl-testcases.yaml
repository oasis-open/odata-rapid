# test cases for rsdl.abnf
- input: "service{foo:bar}"
  expect: [service, singleton, foo, bar] # parsed tokens - TODO: finalize format
- input: "servicex{foo:bar}"
  failAt: 7 # intentionally invalid input
- input: "service x { foo : bar }"
  expect: [service, x, singleton, foo, bar] # parsed tokens - TODO: finalize format
- input: |
    service x { 
      foo: bar
    }
  expect: [service, x, singleton, foo, bar] # parsed tokens - TODO: finalize format
